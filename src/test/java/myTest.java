/**
 * Created by nallgood on 2/14/17.
 */

import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

import java.util.ArrayList;
import java.util.List;
import java.util.*;
import java.io.*;

import static org.junit.Assert.assertEquals;

@RunWith(JUnit4.class)


public class myTest {

    private String userInput = "8 2 3 4 99 100 11";

    //List<String> splitInput = new ArrayList<>();


    //List<String> splitInput = Arrays.asList(userInput.split("\\s+");


    @Test
    public void TestRunner(){

        //assertEquals();

        // Write GUI - not ests - class
        // Create new binary trees - GENERIC class , recursive
            // Make Tree
            // Add to tree .. must be put in right sorted place
            // Inorder Traverse

        // Fractions Class - override toString() method
           // no spaces needed on slash, split on /
         // Implements Comparable
        // must have compareTo() method
        // Throws number formatException

    }
}
